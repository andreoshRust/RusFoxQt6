//Итоги тестирования QtIODevice (QtIOУстройство):
//✅ Все тесты пройдены успешно (7 из 7)
//Что было протестировано:
//✅ Создание объекта QtIODeviceTest
//✅ Открытие и закрытие устройства (Открыть, Закрыть, Открыто)
//✅ Чтение данных (ПрочитатьВсе, Доступно) - прочитано 38 байт
//✅ Запись данных (Записать, ЗаписатьСтроку, ЗаписатьСимвол)
//✅ Позиционирование (Позиция, ПерейтиВ, Сбросить, Размер)
//✅ Ошибки (УстановитьСтрокуОшибки, СтрокаОшибки)
//✅ Проверка RusFunc (42 метода в JSON)
//Диагностика показала:
//24 английских метода доступны
//27 русских методов доступны
//Метод RusFunc() работает корректно (JSON длиной 3320, 42 элемента)
//Класс QtIODevice предоставляет:
//Управление устройством - Открыть, Закрыть, Открыто
//Чтение данных - Прочитать, ПрочитатьВсе, ПрочитатьСтроку, ПрочитатьСимвол
//Запись данных - Записать, ЗаписатьСтроку, ЗаписатьСимвол
//Позиционирование - Позиция, ПерейтиВ, Сбросить, ВКонце
//Информация о данных - Размер, Доступно, ЕстьДанныеДляЗаписи
//Ожидание - ЖдатьЧтения
//Ошибки - СтрокаОшибки, УстановитьСтрокуОшибки
//Режимы открытия - Режим_ТолькоЧтение, Режим_ТолькоЗапись, Режим_ЧтениеЗапись, Режим_Добавление, Режим_Очистка, Режим_Текст, Режим_НеБлокирующий
//Вывод:
//Класс QtIODevice (и его тестовый наследник QtIODeviceTest) полностью функционален и готов к использованию. Он предоставляет удобный интерфейс для 
//работы с устройствами ввода-вывода как на английском, так и на русском языке. Класс успешно наследуется от QtObject и интегрирован в систему отладки.

// ============================================================
// ФУНКЦИЯ ДИАГНОСТИКИ МЕТОДОВ QtIODevice
// ============================================================
Пусть Режим_Отладки = ИСТИНА

Функция ДиагностикаМетодовQtIODevice()
    ВывестиСтрокуВКонсоль("=== ДИАГНОСТИКА МЕТОДОВ QtIOУстройство ===")
    
    Попытка
        ВывестиСтрокуВКонсоль("Проверка существования QtIODeviceTest: " + (typeof QtIODeviceTest !== 'undefined'))
    Исключение
        ВывестиСтрокуВКонсоль("ОШИБКА при проверке QtIODeviceTest: " + e)
    КонецПопытки;
    
    Попытка
        диагУстройство = Новый QtIODeviceTest()
        Если диагУстройство == null Тогда
            ВывестиСтрокуВКонсоль("✗ ОШИБКА: QtIODeviceTest не создан")
            Возврат 
        КонецЕсли;
        
        диагУстройство.РежимОтладки(Режим_Отладки)
        ВывестиСтрокуВКонсоль("✓ QtIOУстройство создан (через QtIODeviceTest)")
        
        // Проверка английских методов
        ВывестиСтрокуВКонсоль("Английские методы (должны работать):")
        англМетоды = [
            "openDevice", "closeDevice", "isOpenDevice",
            "readData", "readAllData", "readLineData", "getCharData",
            "bytesAvailableData", "waitForReadyReadData",
            "writeData", "writeLineData", "putCharData",
            "bytesToWriteData", "posData", "sizeData",
            "seekData", "atEndData", "resetDevice",
            "errorStringData", "setErrorStringData",
            "isValid", "getDevice",
            "getBuffer", "setBuffer"
        ]
        
        рабочиеАнгл = 0
        Для i = 0 По англМетоды.length - 1 Цикл
            Попытка
                Если typeof диагУстройство[англМетоды[i]] === 'function' Тогда
                    рабочиеАнгл = рабочиеАнгл + 1
                    ВывестиСтрокуВКонсоль("  ✓ " + англМетоды[i])
                Иначе
                    ВывестиСтрокуВКонсоль("  ✗ " + англМетоды[i] + " (НЕ ДОСТУПЕН)")
                КонецЕсли;
            Исключение
                ВывестиСтрокуВКонсоль("  ✗ " + англМетоды[i] + " (ОШИБКА: " + e + ")")
            КонецПопытки;
        КонецЦикла;
        ВывестиСтрокуВКонсоль("  Итого: " + рабочиеАнгл + " из " + англМетоды.length + " английских методов доступно")
        
        // Проверка русских методов через RusFunc
        ВывестиСтрокуВКонсоль("Русские методы (должны создаваться через RusFunc):")
        русМетоды = [
            "Открыть", "Закрыть", "Открыто",
            "Прочитать", "ПрочитатьВсе", "ПрочитатьСтроку", "ПрочитатьСимвол",
            "Доступно", "ЖдатьЧтения",
            "Записать", "ЗаписатьСтроку", "ЗаписатьСимвол",
            "ЕстьДанныеДляЗаписи", "Позиция", "Размер",
            "ПерейтиВ", "ВКонце", "Сбросить",
            "СтрокаОшибки", "УстановитьСтрокуОшибки",
            "Режим_ТолькоЧтение", "Режим_ТолькоЗапись", "Режим_ЧтениеЗапись",
            "Режим_Добавление", "Режим_Очистка", "Режим_Текст", "Режим_НеБлокирующий"
        ]
        
        рабочиеРус = 0
        Для i = 0 По русМетоды.length - 1 Цикл
            Попытка
                Если typeof диагУстройство[русМетоды[i]] === 'function' Тогда
                    рабочиеРус = рабочиеРус + 1
                    ВывестиСтрокуВКонсоль("  ✓ " + русМетоды[i])
                Иначе
                    ВывестиСтрокуВКонсоль("  ✗ " + русМетоды[i] + " (НЕ ДОСТУПЕН)")
                КонецЕсли;
            Исключение
                ВывестиСтрокуВКонсоль("  ✗ " + русМетоды[i] + " (ОШИБКА: " + e + ")")
            КонецПопытки;
        КонецЦикла;
        ВывестиСтрокуВКонсоль("  Итого: " + рабочиеРус + " из " + русМетоды.length + " русских методов доступно")
        
        // Проверка RusFunc
        Попытка
            Если typeof диагУстройство.RusFunc === 'function' Тогда
                ВывестиСтрокуВКонсоль("✓ Метод RusFunc() существует")
                Попытка
                    jsonStr = диагУстройство.RusFunc()
                    ВывестиСтрокуВКонсоль("✓ RusFunc() вызван, длина JSON: " + jsonStr.length)
                    карта = JSON.parse(jsonStr)
                    ВывестиСтрокуВКонсоль("✓ JSON успешно распарсен, элементов: " + карта.length)
                Исключение
                    ВывестиСтрокуВКонсоль("✗ Ошибка при вызове RusFunc(): " + e)
                КонецПопытки;
            Иначе
                ВывестиСтрокуВКонсоль("✗ Метод RusFunc() НЕ СУЩЕСТВУЕТ")
            КонецЕсли;
        Исключение
            ВывестиСтрокуВКонсоль("✗ Ошибка при проверке RusFunc(): " + e)
        КонецПопытки;
        
    Исключение
        ВывестиСтрокуВКонсоль("✗ ОШИБКА при диагностике QtIODevice: " + e)
    КонецПопытки;
    
    ВывестиСтрокуВКонсоль("=== КОНЕЦ ДИАГНОСТИКИ ===")
    ВывестиСтрокуВКонсоль("")
КонецФункции

// ============================================================
// ДИАГНОСТИКА МЕТА-ОБЪЕКТА QtIODevice
// ============================================================

Функция ДиагностикаМетаОбъектаIODevice()
    ВывестиСтрокуВКонсоль("=== ДИАГНОСТИКА МЕТА-ОБЪЕКТА QtIOУстройство ===")
    
    Попытка
        ВывестиСтрокуВКонсоль("Проверка QtIODeviceTest: " + (typeof QtIODeviceTest !== 'undefined'))
        ВывестиСтрокуВКонсоль("Проверка QtIODevice: " + (typeof QtIODevice !== 'undefined'))
    Исключение
        ВывестиСтрокуВКонсоль("Ошибка при проверке: " + e)
    КонецПопытки
    
    Попытка
        устройство = Новый QtIODeviceTest()
        Если устройство == null Тогда
            устройство = new QtIODeviceTest()
            Если устройство == null Тогда
                ВывестиСтрокуВКонсоль("✗ Объект не создан")
                Возврат 
            Иначе
                ВывестиСтрокуВКонсоль("✓ Объект создан через new QtIODeviceTest()")
            КонецЕсли;
        Иначе
            ВывестиСтрокуВКонсоль("✓ Объект создан через НовыйОбъект")
        КонецЕсли;
    Исключение
        ВывестиСтрокуВКонсоль("✗ Ошибка создания: " + e)
        Возврат
    КонецПопытки
    
    ВывестиСтрокуВКонсоль("")
    ВывестиСтрокуВКонсоль("Все методы объекта QtIOУстройство:")
    методы = Object.getOwnPropertyNames(устройство)
    прототип = Object.getPrototypeOf(устройство)
    методыПрототипа = Object.getOwnPropertyNames(прототип)
    Для i = 0 По методыПрототипа.length - 1 Цикл
        имя = методыПрототипа[i]
        Если методы.indexOf(имя) === -1 Тогда
            методы.push(имя)
        КонецЕсли
    КонецЦикла
    методы.sort()
    
    Для i = 0 По методы.length - 1 Цикл
        имя = методы[i]
        Если typeof устройство[имя] === 'function' Тогда
            Если имя === 'RusFunc' ИЛИ имя === 'setDebug' ИЛИ имя === 'openDevice' Тогда
                УстановитьЦветКонсоли("green")
                ВывестиСтрокуВКонсоль("  ✓ " + имя + "() - НАЙДЕН!")
                СброситьЦветКонсоли()
            Иначе
                ВывестиСтрокуВКонсоль("    " + имя + "()")
            КонецЕсли
        КонецЕсли
    КонецЦикла
    
    ВывестиСтрокуВКонсоль("")
    Попытка
        Если typeof устройство.RusFunc === 'function' Тогда
            УстановитьЦветКонсоли("green")
            ВывестиСтрокуВКонсоль("✓ RusFunc существует!")
            СброситьЦветКонсоли()
            
            jsonStr = устройство.RusFunc()
            карта = JSON.parse(jsonStr)
            ВывестиСтрокуВКонсоль("  Количество методов в JSON: " + карта.length)
            
            ВывестиСтрокуВКонсоль("  Первые 10 методов:")
            Для i = 0 По 9 Цикл
                Если i < карта.length Тогда
                    item = карта[i]
                    ВывестиСтрокуВКонсоль("    " + item.русскоеИмя + " -> " + item.латинскоеИмя)
                КонецЕсли
            КонецЦикла
        Иначе
            УстановитьЦветКонсоли("red")
            ВывестиСтрокуВКонсоль("✗ RusFunc НЕ СУЩЕСТВУЕТ!")
            СброситьЦветКонсоли()
        КонецЕсли
    Исключение
        УстановитьЦветКонсоли("red")
        ВывестиСтрокуВКонсоль("✗ Ошибка при проверке RusFunc: " + e)
        СброситьЦветКонсоли()
    КонецПопытки
    
    ВывестиСтрокуВКонсоль("=== КОНЕЦ ДИАГНОСТИКИ ===")
    ВывестиСтрокуВКонсоль("")
КонецФункции

// ============================================================
// ТЕСТ QtIODevice (QtIODeviceTest)
// ============================================================

Функция ТестQtIODeviceFull()
    всегоТестов = 0
    успешноТестов = 0
    
    // ===== 1. СОЗДАНИЕ ОБЪЕКТА =====
    всегоТестов = всегоТестов + 1
    УстановитьЦветКонсоли("yellow")
    ВывестиСтрокуВКонсоль("  Тест 1: Создание объекта")
    СброситьЦветКонсоли()
    
    устройство = null
    Попытка
        устройство = Новый QtIODeviceTest()
        Если устройство != null Тогда
            устройство.РежимОтладки(Режим_Отладки)
            УстановитьЦветКонсоли("green")
            ВывестиСтрокуВКонсоль("    ✓ QtIOУстройство создан (через QtIODeviceTest)")
            СброситьЦветКонсоли()
            успешноТестов = успешноТестов + 1
        Иначе
            УстановитьЦветКонсоли("red")
            ВывестиСтрокуВКонсоль("    ✗ QtIOУстройство НЕ создан")
            СброситьЦветКонсоли()
        КонецЕсли;
    Исключение
        УстановитьЦветКонсоли("red")
        ВывестиСтрокуВКонсоль("    ✗ Ошибка создания: " + e)
        СброситьЦветКонсоли()
    КонецПопытки;
    
    Если устройство == null Тогда
        Возврат "❌ Тест провален (объект не создан)"
    КонецЕсли

    // ===== 2. ОТКРЫТИЕ И ЗАКРЫТИЕ =====
    всегоТестов = всегоТестов + 1
    УстановитьЦветКонсоли("yellow")
    ВывестиСтрокуВКонсоль("  Тест 2: Открытие и закрытие")
    СброситьЦветКонсоли()
    
    тестПройден = ИСТИНА
    
    Попытка
        результат = устройство.Открыть(устройство.Режим_ЧтениеЗапись())
        ВывестиСтрокуВКонсоль("    Открыть(Режим_ЧтениеЗапись): " + результат)
        
        открыто = устройство.Открыто()
        ВывестиСтрокуВКонсоль("    Открыто(): " + открыто)
        
        Если открыто == ИСТИНА Тогда
            УстановитьЦветКонсоли("green")
            ВывестиСтрокуВКонсоль("    ✓ Устройство открыто")
            СброситьЦветКонсоли()
        Иначе
            УстановитьЦветКонсоли("red")
            ВывестиСтрокуВКонсоль("    ✗ Устройство НЕ открыто")
            СброситьЦветКонсоли()
            тестПройден = ЛОЖЬ
        КонецЕсли
    Исключение
        УстановитьЦветКонсоли("red")
        ВывестиСтрокуВКонсоль("    ✗ Ошибка: " + e)
        СброситьЦветКонсоли()
        тестПройден = ЛОЖЬ
    КонецПопытки
    
    Если тестПройден Тогда
        Попытка
            устройство.Закрыть()
            открытоПосле = устройство.Открыто()
            ВывестиСтрокуВКонсоль("    После Закрыть(): " + открытоПосле)
            
            Если открытоПосле == ЛОЖЬ Тогда
                УстановитьЦветКонсоли("green")
                ВывестиСтрокуВКонсоль("    ✓ Открытие и закрытие работают")
                СброситьЦветКонсоли()
                успешноТестов = успешноТестов + 1
            Иначе
                УстановитьЦветКонсоли("red")
                ВывестиСтрокуВКонсоль("    ✗ Закрытие не работает")
                СброситьЦветКонсоли()
            КонецЕсли
        Исключение
            УстановитьЦветКонсоли("red")
            ВывестиСтрокуВКонсоль("    ✗ Ошибка: " + e)
            СброситьЦветКонсоли()
        КонецПопытки
    КонецЕсли

    // ===== 3. ЧТЕНИЕ =====
    всегоТестов = всегоТестов + 1
    УстановитьЦветКонсоли("yellow")
    ВывестиСтрокуВКонсоль("  Тест 3: Чтение")
    СброситьЦветКонсоли()
    
    тестПройден = ИСТИНА
    
    Попытка
        // Открываем устройство и сбрасываем позицию
        устройство.Открыть(устройство.Режим_ЧтениеЗапись())
        устройство.Сбросить()
        
        // Проверяем размер буфера
        размерБуфера = устройство.Размер()
        ВывестиСтрокуВКонсоль("    Размер буфера: " + размерБуфера)
        
        Если размерБуфера > 0 Тогда
            // Читаем все данные
            прочитано = устройство.ПрочитатьВсе()
            доступно = устройство.Доступно()
            
            // Преобразуем QByteArray в строку для вывода
            прочитаноСтрока = прочитано.toString()
            ВывестиСтрокуВКонсоль("    ПрочитатьВсе(): " + прочитаноСтрока)
            ВывестиСтрокуВКонсоль("    Размер прочитанного: " + прочитаноСтрока.length)
            ВывестиСтрокуВКонсоль("    Доступно(): " + доступно)
            
            Если прочитаноСтрока != "" И прочитаноСтрока.length > 0 Тогда
                УстановитьЦветКонсоли("green")
                ВывестиСтрокуВКонсоль("    ✓ Чтение работает")
                СброситьЦветКонсоли()
                успешноТестов = успешноТестов + 1
            Иначе
                УстановитьЦветКонсоли("red")
                ВывестиСтрокуВКонсоль("    ✗ Чтение не работает (прочитано пусто)")
                СброситьЦветКонсоли()
            КонецЕсли
        Иначе
            // Если буфер пуст, заполняем его
            устройство.setBuffer("Тестовые данные для чтения\nСтрока 2\nСтрока 3")
            устройство.Сбросить()
            
            прочитано2 = устройство.ПрочитатьВсе()
            прочитаноСтрока2 = прочитано2.toString()
            ВывестиСтрокуВКонсоль("    Буфер заполнен, ПрочитатьВсе(): " + прочитаноСтрока2)
            
            Если прочитаноСтрока2 != "" И прочитаноСтрока2.length > 0 Тогда
                УстановитьЦветКонсоли("green")
                ВывестиСтрокуВКонсоль("    ✓ Чтение работает (после заполнения буфера)")
                СброситьЦветКонсоли()
                успешноТестов = успешноТестов + 1
            Иначе
                УстановитьЦветКонсоли("red")
                ВывестиСтрокуВКонсоль("    ✗ Чтение не работает даже после заполнения буфера")
                СброситьЦветКонсоли()
            КонецЕсли
        КонецЕсли
    Исключение
        УстановитьЦветКонсоли("red")
        ВывестиСтрокуВКонсоль("    ✗ Ошибка: " + e)
        СброситьЦветКонсоли()
    КонецПопытки

    // ===== 4. ЗАПИСЬ =====
    всегоТестов = всегоТестов + 1
    УстановитьЦветКонсоли("yellow")
    ВывестиСтрокуВКонсоль("  Тест 4: Запись")
    СброситьЦветКонсоли()
    
    тестПройден = ИСТИНА
    
    Попытка
        устройство.Открыть(устройство.Режим_ЧтениеЗапись())
        устройство.Сбросить()
        
        записано1 = устройство.Записать("Hello World")
        записано2 = устройство.ЗаписатьСтроку("Line 2")
        записано3 = устройство.ЗаписатьСимвол('!')
        
        буфер = устройство.getBuffer()
        ВывестиСтрокуВКонсоль("    Записаны: 'Hello World', 'Line 2', '!'")
        ВывестиСтрокуВКонсоль("    Буфер после записи: " + буфер)
        
        Если записано1 > 0 И записано2 > 0 И записано3 == true Тогда
            УстановитьЦветКонсоли("green")
            ВывестиСтрокуВКонсоль("    ✓ Запись работает")
            СброситьЦветКонсоли()
            успешноТестов = успешноТестов + 1
        Иначе
            УстановитьЦветКонсоли("red")
            ВывестиСтрокуВКонсоль("    ✗ Запись не работает")
            СброситьЦветКонсоли()
        КонецЕсли
    Исключение
        УстановитьЦветКонсоли("red")
        ВывестиСтрокуВКонсоль("    ✗ Ошибка: " + e)
        СброситьЦветКонсоли()
    КонецПопытки

    // ===== 5. ПОЗИЦИОНИРОВАНИЕ =====
    всегоТестов = всегоТестов + 1
    УстановитьЦветКонсоли("yellow")
    ВывестиСтрокуВКонсоль("  Тест 5: Позиционирование")
    СброситьЦветКонсоли()
    
    тестПройден = ИСТИНА
    
    Попытка
        устройство.setBuffer("0123456789")
        устройство.Открыть(устройство.Режим_ЧтениеЗапись())
        
        позиция = устройство.Позиция()
        размер = устройство.Размер()
        ВывестиСтрокуВКонсоль("    Позиция(): " + позиция + ", Размер(): " + размер)
        
        устройство.ПерейтиВ(5)
        позиция2 = устройство.Позиция()
        ВывестиСтрокуВКонсоль("    ПерейтиВ(5) -> Позиция(): " + позиция2)
        
        устройство.Сбросить()
        позиция3 = устройство.Позиция()
        ВывестиСтрокуВКонсоль("    Сбросить() -> Позиция(): " + позиция3)
        
        Если позиция == 0 И позиция2 == 5 И позиция3 == 0 Тогда
            УстановитьЦветКонсоли("green")
            ВывестиСтрокуВКонсоль("    ✓ Позиционирование работает")
            СброситьЦветКонсоли()
            успешноТестов = успешноТестов + 1
        Иначе
            УстановитьЦветКонсоли("red")
            ВывестиСтрокуВКонсоль("    ✗ Позиционирование не работает")
            СброситьЦветКонсоли()
        КонецЕсли
    Исключение
        УстановитьЦветКонсоли("red")
        ВывестиСтрокуВКонсоль("    ✗ Ошибка: " + e)
        СброситьЦветКонсоли()
    КонецПопытки

    // ===== 6. ОШИБКИ =====
    всегоТестов = всегоТестов + 1
    УстановитьЦветКонсоли("yellow")
    ВывестиСтрокуВКонсоль("  Тест 6: Ошибки")
    СброситьЦветКонсоли()
    
    тестПройден = ИСТИНА
    
    Попытка
        устройство.УстановитьСтрокуОшибки("Тестовая ошибка")
        ошибка = устройство.СтрокаОшибки()
        ВывестиСтрокуВКонсоль("    УстановитьСтрокуОшибки() -> СтрокаОшибки(): " + ошибка)
        
        Если ошибка == "Тестовая ошибка" Тогда
            УстановитьЦветКонсоли("green")
            ВывестиСтрокуВКонсоль("    ✓ Установка/получение ошибки работает")
            СброситьЦветКонсоли()
            успешноТестов = успешноТестов + 1
        Иначе
            УстановитьЦветКонсоли("red")
            ВывестиСтрокуВКонсоль("    ✗ Установка/получение ошибки не работает")
            СброситьЦветКонсоли()
        КонецЕсли
    Исключение
        УстановитьЦветКонсоли("red")
        ВывестиСтрокуВКонсоль("    ✗ Ошибка: " + e)
        СброситьЦветКонсоли()
    КонецПопытки

    // ===== 7. ПРОВЕРКА RUSFUNC =====
    всегоТестов = всегоТестов + 1
    УстановитьЦветКонсоли("yellow")
    ВывестиСтрокуВКонсоль("  Тест 7: Проверка RusFunc")
    СброситьЦветКонсоли()
    
    Попытка
        Если typeof устройство.RusFunc === 'function' Тогда
            jsonStr = устройство.RusFunc()
            карта = JSON.parse(jsonStr)
            Если карта.length > 0 Тогда
                УстановитьЦветКонсоли("green")
                ВывестиСтрокуВКонсоль("    ✓ [RUS] RusFunc возвращает валидный JSON (" + карта.length + " методов)")
                СброситьЦветКонсоли()
                успешноТестов = успешноТестов + 1
            Иначе
                УстановитьЦветКонсоли("red")
                ВывестиСтрокуВКонсоль("    ✗ [RUS] RusFunc вернул пустой JSON")
                СброситьЦветКонсоли()
            КонецЕсли
        Иначе
            УстановитьЦветКонсоли("red")
            ВывестиСтрокуВКонсоль("    ✗ [RUS] Метод RusFunc НЕ СУЩЕСТВУЕТ")
            СброситьЦветКонсоли()
        КонецЕсли
    Исключение
        УстановитьЦветКонсоли("red")
        ВывестиСтрокуВКонсоль("    ✗ [RUS] Ошибка: " + e)
        СброситьЦветКонсоли()
    КонецПопытки

    // ===== ИТОГ =====
    ВывестиСтрокуВКонсоль("")
    УстановитьЦветКонсоли("cyan")
    ВывестиСтрокуВКонсоль("┌────────────────────────────────────────────────────────────────────────────────┐")
    ВывестиСтрокуВКонсоль("│                         РЕЗУЛЬТАТЫ ТЕСТИРОВАНИЯ                              │")
    ВывестиСтрокуВКонсоль("├────────────────────────────────────────────────────────────────────────────────┤")
    ВывестиСтрокуВКонсоль("│  Всего тестов: " + всегоТестов + "                                                       │")
    
    Если успешноТестов == всегоТестов Тогда
        УстановитьЦветКонсоли("green")
        ВывестиСтрокуВКонсоль("│  ✅ Успешно:   " + успешноТестов + " (ВСЕ ТЕСТЫ ПРОЙДЕНЫ!)                    │")
        СброситьЦветКонсоли()
    Иначе
        УстановитьЦветКонсоли("yellow")
        ВывестиСтрокуВКонсоль("│  ✅ Успешно:   " + успешноТестов + "                                      │")
        УстановитьЦветКонсоли("red")
        ВывестиСтрокуВКонсоль("│  ❌ Провалено: " + (всегоТестов - успешноТестов) + "                                     │")
        СброситьЦветКонсоли()
    КонецЕсли
    
    ВывестиСтрокуВКонсоль("└────────────────────────────────────────────────────────────────────────────────┘")
    СброситьЦветКонсоли()
    
    Возврат "Тест QtIOУстройство завершён: " + успешноТестов + " из " + всегоТестов
КонецФункции

// ============================================================
// ГЛАВНАЯ ПРОЦЕДУРА
// ============================================================

ПРОЦЕДУРА Старт()
//#ОтладкаПрепроцессора

    Пусть НомерВерсииПрограммы = ВерсияПрограммы()
    Если НомерВерсииПрограммы < 2.12 Тогда 
        ПоказатьСообщение("Данный сценарий корректно работает начиная с версии 2.12. Ваша версия программы " + НомерВерсииПрограммы +
           ". Обновите версию на https://github.com/andreoshRust/RusFoxQt6 ")
        Возврат    
    КонецЕсли;
    
    ПодавитьВыводОкна(ИСТИНА)
    СкрытьГлавноеОкно()
    ОчиститьКонсоль()
    
    УстановитьЦветКонсоли("cyan")
    ВывестиСтрокуВКонсоль("╔════════════════════════════════════════════════════════════════════════════════╗")
    ВывестиСтрокуВКонсоль("║               ТЕСТИРОВАНИЕ QtIOУстройство (QtIODevice)                        ║")
    ВывестиСтрокуВКонсоль("╚════════════════════════════════════════════════════════════════════════════════╝")
    СброситьЦветКонсоли()
    ВывестиСтрокуВКонсоль("")
    
    // ========== ДИАГНОСТИКА ==========
    ДиагностикаМетодовQtIODevice()
    ДиагностикаМетаОбъектаIODevice()
    
    // ========== ТЕСТЫ ==========
    УстановитьЦветКонсоли("yellow")
    ВывестиСтрокуВКонсоль("┌────────────────────────────────────────────────────────────────────────────────┐")
    ВывестиСтрокуВКонсоль("│ ТЕСТ QtIOУстройство (русские и английские методы)                           │")
    ВывестиСтрокуВКонсоль("└────────────────────────────────────────────────────────────────────────────────┘")
    СброситьЦветКонсоли()
    
    Попытка
        результат = ТестQtIODeviceFull()
        УстановитьЦветКонсоли("green")
        ВывестиСтрокуВКонсоль(результат)
    Исключение
        УстановитьЦветКонсоли("red")
        ВывестиСтрокуВКонсоль("❌ Ошибка в тесте QtIODevice: " + e)
    КонецПопытки;
    СброситьЦветКонсоли()
    ВывестиСтрокуВКонсоль("")
    
    // ========== ИТОГ ==========
    УстановитьЦветКонсоли("cyan")
    ВывестиСтрокуВКонсоль("╔════════════════════════════════════════════════════════════════════════════════╗")
    ВывестиСтрокуВКонсоль("║                         ТЕСТИРОВАНИЕ ЗАВЕРШЕНО                                 ║")
    ВывестиСтрокуВКонсоль("╚════════════════════════════════════════════════════════════════════════════════╝")
    СброситьЦветКонсоли()
    ВывестиСтрокуВКонсоль("")
    
    ВывестиСтрокуВКонсоль("Тестирование завершено. Программа закроется через 10 секунд.")
    Пауза(10000)
КОНЕЦПРОЦЕДУРЫ

